# wechat-article-skill

微信公众号文章创作 Skill —— 从主题到草稿箱，一句话搞定。

## 功能

- ✍️ **AI 文章创作** — 只需一句主题，自动按你的风格生成完整文章
- 📝 **排版格式化** — 内联 CSS 的 HTML 模板，适配公众号，手机阅读体验优秀
- 🎨 **封面图生成** — 浅色背景 + 格子纹理 + 思源黑体 Bold，简洁有设计感
- 🚀 **草稿推送** — 自动上传封面、创建草稿，一条命令搞定

## 使用方式

### 首次使用

第一次触发时，agent 会通过对话引导你完成配置：

- 公众号 AppID 和 AppSecret
- 默认作者名
- 写作风格偏好（视角、语气、长度、方向）

配置信息保存在本地 `wechat-article.config.json`，后续自动读取。

### 日常使用

配置完成后，只需发一句话：

> "帮我写一篇公众号文章，聊聊为什么AI让创新变简单了"

Agent 会自动完成：创作文章 → 排版 → 生成封面图 → 推送到草稿箱。

你只需去公众号后台预览确认后发布。

### 修改配置

随时可以说：

> "修改公众号配置" / "更新写作风格"

## 排版风格

- 16px 正文，2倍行高，两端对齐
- 短句换行，段落留白，阅读节奏舒适
- 蓝色加粗（`#1a73e8`）突出重点观点
- h2 章节标题 + 蓝色下划线分隔
- 浅灰分隔线划分文章段落

## 项目结构

```
wechat-article/
├── SKILL.md                      # Skill 说明（工作流程）
├── README.md                     # 仓库介绍
├── references/
│   └── article-style.md          # 排版样式规范
├── scripts/
│   ├── create_cover.py           # 封面图生成脚本
│   └── publish_draft.py          # 草稿推送脚本
└── assets/
    └── NotoSansCJKsc-Bold.otf    # 思源黑体 Bold（开源免费）
```

## 独立使用（不依赖 OpenClaw）

### 安装依赖

```bash
pip3 install Pillow
```

### 配置环境变量

```bash
export WX_APPID="你的AppID"
export WX_APPSECRET="你的AppSecret"
export WX_AUTHOR="作者名"
```

### 生成封面图

```bash
python3 scripts/create_cover.py \
  --title "文章主标题" \
  --subtitle "副标题" \
  --output cover.jpg
```

### 推送到草稿箱

```bash
python3 scripts/publish_draft.py \
  --title "文章标题" \
  --digest "文章摘要" \
  --content-file article.html \
  --cover cover.jpg
```

## 注意事项

- 仅推送到草稿箱，**不会直接发布**
- 需要在公众号后台将服务器 IP 加入白名单
- 封面图尺寸 900×383（2.35:1）
- 字体使用[思源黑体](https://github.com/googlefonts/noto-cjk)（SIL Open Font License），可自由商用

## License

MIT
