# wechat-article-skill

微信公众号文章创作 Skill（OpenClaw）

目标很明确：把“写文章 + 做封面 + 发草稿”做成**稳定、可预览、可确认**的一条链路，减少返工。

---

## 这版核心能力

- ✍️ **主题直出文章**：按你的写作偏好生成公众号正文
- 🧩 **公众号友好排版**：HTML 内联样式，适配微信编辑器
- 🎨 **封面风格×配色**：4 种风格 + 6 套配色，统一调性又避免重复
- 🖼️ **首次配置可视化选择**：固定预览拼图，中文编号选择
- 👀 **发布前图文预览确认**：先看“正文预览+封面图”，确认后才推草稿
- 🚀 **草稿箱发布**：自动上传封面并创建草稿（不直接群发）

---

## 给 OpenClaw 用户的使用引导（重点）

> 这部分是给“别人的 OpenClaw”看的，照着做即可。

### 1) 安装 Skill

把仓库放到 OpenClaw 工作区 skills 目录下，目录名建议保持：

```bash
~/.openclaw/workspace/skills/wechat-article-skill
```

确保至少包含：

- `SKILL.md`
- `scripts/create_cover.py`
- `scripts/create_cover_preview_grid.py`
- `scripts/publish_draft.py`
- `assets/NotoSansCJKsc-Bold.otf`
- `assets/cover-style-palette-preview-grid.jpg`

### 2) 在对话里触发

对 OpenClaw 说类似下面的话即可触发：

- “帮我写一篇公众号文章，主题是 XXX”
- “给这篇文章做公众号封面并推送草稿”
- “修改公众号配置/更新封面风格”

### 3) 首次配置会发生什么

首次使用会引导你填写：

1. `AppID` / `AppSecret`
2. 默认作者
3. 写作风格偏好
4. 评论开关
5. 封面默认策略（先看预览图再选）

系统会发固定预览图 `assets/cover-style-palette-preview-grid.jpg`，并让你按中文编号选择：

- A 风格（A1~A4）
- B 配色策略（自动/固定）
- C 固定配色（C1~C6，仅 B2）
- D 轮换方式（顺序/随机，仅 B1）

示例回复：

- `A2 B1 D1`（编辑卡片 + 自动配色 + 顺序轮换）
- `A1 B2 C3`（极简网格 + 固定增长绿）

### 4) 日常发布流程

后续每次会走这条链路：

**创作正文 → 生成封面 → 发送图文预览 → 你确认 → 推送草稿箱**

预览阶段可直接回复：

- `确认发布`
- `修改封面`
- `修改正文`

> 默认未收到“确认发布”不会执行草稿推送。

---

## 封面系统说明

### 风格（Style）

1. `minimal-grid`（极简网格）
2. `card-editorial`（编辑卡片）
3. `diagonal-motion`（斜切动势）
4. `soft-gradient`（柔和渐变）

### 配色（Palette）

1. `blue-tech`（科技蓝）
2. `purple-insight`（洞察紫）
3. `green-growth`（增长绿）
4. `orange-energy`（活力橙）
5. `rose-story`（故事玫红）
6. `slate-pro`（专业灰）

### 预览图生成（可选）

如需重建预览拼图：

```bash
python3 scripts/create_cover_preview_grid.py
```

---

## 配置文件示例（wechat-article.config.json）

```json
{
  "appid": "公众号 AppID",
  "appsecret": "公众号 AppSecret",
  "author": "默认作者",
  "writing": {
    "perspective": "第三人称与行业观察视角",
    "tone": "直接、有冲击力，但不夸张",
    "length": "1200-2200字",
    "direction": "科技自媒体，聚焦AI行业观点",
    "keywords_style": "短句为主，一行不超过30字"
  },
  "publish": {
    "need_open_comment": 1,
    "only_fans_can_comment": 0
  },
  "cover": {
    "default_style": "minimal-grid",
    "palette": "auto",
    "rotate": "sequential",
    "seed": "title"
  },
  "preview": {
    "send_cover_preview": 1,
    "require_confirm_before_publish": 1,
    "confirm_keyword": "确认发布"
  }
}
```

---

## 独立脚本使用（不依赖 OpenClaw）

### 安装依赖

```bash
pip3 install Pillow
```

### 1) 生成封面

```bash
python3 scripts/create_cover.py \
  --title "文章主标题" \
  --subtitle "副标题" \
  --style minimal-grid \
  --palette auto \
  --rotate sequential \
  --output cover.jpg
```

### 2) 推送草稿

```bash
python3 scripts/publish_draft.py \
  --title "文章标题" \
  --author "作者名" \
  --digest "文章摘要" \
  --content-file article.html \
  --cover cover.jpg \
  --appid "你的AppID" \
  --appsecret "你的AppSecret" \
  --need-open-comment 1 \
  --only-fans-can-comment 0
```

也支持环境变量：

- `WX_APPID`
- `WX_APPSECRET`
- `WX_AUTHOR`

---

## 注意事项

- 只推送到草稿箱，不直接群发
- 需要在公众号后台将服务器 IP 加白名单
- 封面尺寸为 `900×383`
- 发布前建议始终看“正文+封面”预览再确认

---

## License

MIT
